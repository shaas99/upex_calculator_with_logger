/* eslint-disable no-console */
// eslint-disable-next-line import/prefer-default-export
export const logger = {
  log: (message: string) => {
    console.log('[Upex Lib Template]', message);
  },
};
